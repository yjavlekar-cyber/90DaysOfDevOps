# Linux Architecture
    1.Linux is an operating system devloped by linus torwards in 1991 and more than 90 percent of apps run on linux.
    
    2.Everything on linux is either a file or a directory.
    
    3.linux architecture consist hardware,kernel a program which will communicate with hardware,
        shell which is an interactive way to talk to kernel and then application which will be the end user like us.
        
    4.linux has following distributors 
      - 1)Ubuntu 
      - 2)Fedora
      - 3)cent os 
      - 4)redhat-rhel.
      
    5.the first process of linux will be power on steps of which are 
        - 1.you power on the system 
        - 2.B.I.O.S supplies power to motherboard to load the hardware 
        - 3.GNU GRUB(grand unified bootloder)hardware then signal to gnu to load linux kernel.
        
    6.then the system strts all this is called init process/systemD this will be the very first process which is reflected on linux which is reflected with PID no PID1.
    7.The very basic five commands which i will be using on daily basis are 
    - 1)CD-change directory 
    - 2)pwd-Present working directory 
    - 3)ls-list files 
    - 4)cd .. to go back 
    - 5) touch- to create new file 
    - 6) mkdir to create new directory.
    
    8.The very first step of using linux will be creating the ec2 instance once the instance is created we connect this and 
    we will be presented with the shell which we will be using to operate nd give command on linux.
    
    9.As mentioned earlier that linux is either file or directory the linux system starts with the root directory which is denoted by "/".
    
    10.man command will help us to get more information on the cmd which we want to use man refers
    to manual for eg man cp will open up manual of copy command which will have steps to use the cp command.
